#include <stdio.h>
#include <math.h>
int main()
{
 int num, j, i=0, bin[10];
printf("Enter a decimal number: ");
 scanf("%d", &num);
 while(num>0)
{
bin[i]=num%2;
num=num/2;
i++;
 }
for(j=i-1;j>=0;j--)
{
 printf("%d",bin[j]);
}
return 0;
}
